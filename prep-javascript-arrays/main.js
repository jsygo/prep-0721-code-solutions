const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];
console.log('languages array:', languages);

var firstElement = languages[0];
console.log('first element of languages array:', firstElement);

var thirdElement = languages[2];
console.log('third element of languages array:', thirdElement);

var length = languages.length;
console.log('length of languages array:', length);

var lastElement = languages[languages.length - 1];
console.log('last element of languages array:', lastElement);
